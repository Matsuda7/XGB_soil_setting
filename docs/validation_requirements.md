# 実装状況

N値・γの孔単位holdoutと空間ブロックholdoutをconfigで切り替えるコードを実装。
解析・学習・テストは未実行。詳細はREADMEのvalidation設定を参照。
γはN値を使わない湿潤・乾燥モデルを実装。N値導入比較のコードを実装済み（未実行）。predicted_nでの全域分布接続を実装済み（未実行）、cはCŪ・CDの有効粘着力を使うモデルAを実装済み（未実行）。
将来、予測N値をγに使う評価では、上流モデルからも評価孔・評価地域を除外する。
N値上限50。密度試験とSPTの深度不一致への対応規則は未決定。
前回の対応89地点は深度を含まない（x,y）の小数第3位までの一致。
