# 統合時の検証記録（2026-09-11）

統合先は既存の `XGB_gamma_setting`、プロジェクト表示名は `XGB_soil_distribution`。
元のφ・cプロジェクトのソースとGit履歴はそのまま保存。

- 元データとDEMキャッシュ384ファイル、796,133,107 bytesをハードリンクで取り込み。
  全384ファイルで取込元と取込先が同じ実体であることを確認。
- 土質試験XML316ファイルを解析し、351試験レコードを抽出。XMLエラー0件。
- γ学習用CSV：湿潤333行、乾燥333行。
- c候補CSV：0行。せん断強さを粘着力に代用していない。
- φ前処理：2,607孔から、元のDEMポリシーに従って2,198孔・31,339測定行を作成。
  DEM近傍条件で409孔を除外。地質未一致91孔、J-SHIS未一致26孔。
- 元のφ学習関数と統合後の関数を30孔・120行の同一合成データで比較。
  検証用に両者の木の本数を8、early stoppingを3、並列数を1に揃え、予測表と評価辞書の完全一致を確認。
  リポジトリの本番学習パラメータは元の値を維持。
- 新規テスト6件、既存XMLテスト2件が成功。
- Python構文チェック、`git diff --check` が成功。
- `python run_analysis.py` を実データで通し実行し、収集→整理→分布ディスパッチまで確認。
  終了コード2・状態 `incomplete` は以下の未完了条件を正しく反映したもの。

未検証・未完了：

- `data/raw/grid/output4.txt` と `data/raw/grid/prop.txt` が未配置。
  φの実データでの再学習・全格子予測は実行していない。
- c・γの空間予測モデルは未実装。今回の範囲は統合の枠組みと既存処理の移植。
- ネットワーク経由の再ダウンロードは実行していない。
- 旧リポジトリの削除、GitHubリポジトリ名の変更、commit、pushは行っていない。
